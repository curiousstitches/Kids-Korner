# stories/

Buddy's built-in story library.

- `stories.json` — short stories grouped by age (`age_group`), each with a `title`, the story `content`, and a `words` list of learning words featured in the story.

Add your own stories by copying an existing entry and editing it — keep the same format so the app can read it.
